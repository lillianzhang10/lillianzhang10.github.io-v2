{{ partial "header" . }}

<main>

    <div class="intro">
        <h1>{{ .Site.Title }}</h1>
        <h2>{{ .Site.Params.Description }}</h2>
        {{ .Content }}
    </div>

</main>

{{ partial "footer" . }}
